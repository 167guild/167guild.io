# docker/

Reserved for custom Docker build contexts, helper Dockerfiles, or image-layer assets that are not part of the root Compose file.

Current stack images are referenced directly from `docker-compose.yml`; this directory is intentionally lightweight.
