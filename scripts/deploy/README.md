# scripts/deploy/

This directory is reserved for local deployment helper scripts.

Production deployment scripts currently live in `deploy/scripts/` to keep environment-specific logic colocated with production overlays.
